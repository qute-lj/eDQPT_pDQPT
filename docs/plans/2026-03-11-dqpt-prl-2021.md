# DQPT PRL 2021 Reproduction Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Build and verify a Julia/MPSKit workflow that qualitatively reproduces the 2021 PRL distinction between `pDQPT` and `eDQPT`.

**Architecture:** Implement a reusable infinite-MPS quench driver, then specialize it for an Ising baseline and an entanglement-focused two-site protocol. Keep observables and I/O separate from the evolution loop so that parameter sweeps remain easy to adjust.

**Tech Stack:** Julia, MPSKit, MPSKitModels, TensorKit, Test, DelimitedFiles/JSON3 if needed for output.

---

### Task 1: Create a failing smoke test for helper behavior

**Files:**
- Create: `/home/qute_lj/wsl_workspace/CapsuleEverything/202603/edpqt_pdqpt/test/runtests.jl`
- Create: `/home/qute_lj/wsl_workspace/CapsuleEverything/202603/edpqt_pdqpt/test/test_helpers.jl`
- Test: `/home/qute_lj/wsl_workspace/CapsuleEverything/202603/edpqt_pdqpt/test/test_helpers.jl`

**Step 1: Write the failing test**

Test helper behaviors that do not require long simulations:

- infinite-chain Loschmidt rate is zero when `dot(psi0, psi0)` is used
- output directory creation and parameter normalization behave as expected
- a two-site product-state constructor returns an `InfiniteMPS` with unit-cell length 2

**Step 2: Run test to verify it fails**

Run: `julia --project=. test/runtests.jl`
Expected: FAIL because helper module/script does not yet exist.

**Step 3: Write minimal implementation**

Create a reusable helper module inside the main script or a small included file that defines:

- `loschmidt_rate_infinite`
- `ensure_dir`
- `build_two_site_product_state`

**Step 4: Run test to verify it passes**

Run: `julia --project=. test/runtests.jl`
Expected: PASS.

**Step 5: Commit**

This workspace is not a git repository. Skip commit.

### Task 2: Implement Ising baseline reproduction

**Files:**
- Create: `/home/qute_lj/wsl_workspace/CapsuleEverything/202603/edpqt_pdqpt/scripts/reproduce_dqpt_prl_2021.jl`
- Create: `/home/qute_lj/wsl_workspace/CapsuleEverything/202603/edpqt_pdqpt/src/DQPTPRL2021.jl`
- Modify: `/home/qute_lj/wsl_workspace/CapsuleEverything/202603/edpqt_pdqpt/test/test_helpers.jl`

**Step 1: Write the failing test**

Add a test that calls a short Ising run with a tiny number of steps and checks:

- returned time and rate arrays have matching lengths
- the initial rate is approximately zero
- at least one later point differs from zero

**Step 2: Run test to verify it fails**

Run: `julia --project=. test/runtests.jl`
Expected: FAIL because the Ising driver is not implemented.

**Step 3: Write minimal implementation**

Implement:

- an Ising Hamiltonian builder using `transverse_field_ising`
- `run_ising_baseline(; dt, steps, g0, g1, D0, grow_steps, grow_by, Dmax)`
- measurements for rate, entropy, `sigma_x`, `sigma_z`
- data export to `outputs/ising_baseline_*.tsv`

**Step 4: Run test to verify it passes**

Run: `julia --project=. test/runtests.jl`
Expected: PASS.

**Step 5: Commit**

This workspace is not a git repository. Skip commit.

### Task 3: Implement entanglement-focused second protocol

**Files:**
- Modify: `/home/qute_lj/wsl_workspace/CapsuleEverything/202603/edpqt_pdqpt/src/DQPTPRL2021.jl`
- Modify: `/home/qute_lj/wsl_workspace/CapsuleEverything/202603/edpqt_pdqpt/scripts/reproduce_dqpt_prl_2021.jl`
- Modify: `/home/qute_lj/wsl_workspace/CapsuleEverything/202603/edpqt_pdqpt/test/test_helpers.jl`

**Step 1: Write the failing test**

Add a test for a tiny two-site-unit-cell run checking:

- returned arrays are finite
- entropy is recorded
- the unit cell remains length 2 throughout the setup

**Step 2: Run test to verify it fails**

Run: `julia --project=. test/runtests.jl`
Expected: FAIL because the second protocol driver does not yet exist.

**Step 3: Write minimal implementation**

Implement one entanglement-focused protocol:

- preferred first attempt: XXZ quench with a two-site N\'eel-like product state
- fallback: custom Ising Hamiltonian with extra longitudinal field using `InfiniteMPOHamiltonian`

Measure:

- rate function
- entropy
- local `sigma_z` or staggered magnetization

Write data to `outputs/entanglement_protocol_*.tsv`.

**Step 4: Run test to verify it passes**

Run: `julia --project=. test/runtests.jl`
Expected: PASS.

**Step 5: Commit**

This workspace is not a git repository. Skip commit.

### Task 4: Add run notes and CLI entrypoint

**Files:**
- Modify: `/home/qute_lj/wsl_workspace/CapsuleEverything/202603/edpqt_pdqpt/scripts/reproduce_dqpt_prl_2021.jl`
- Create: `/home/qute_lj/wsl_workspace/CapsuleEverything/202603/edpqt_pdqpt/outputs/README.md`

**Step 1: Write the failing test**

Add a smoke-style test that the CLI argument parser selects the requested mode (`ising`, `xxz`, `all`) and output prefix.

**Step 2: Run test to verify it fails**

Run: `julia --project=. test/runtests.jl`
Expected: FAIL because the CLI parser is incomplete.

**Step 3: Write minimal implementation**

Implement:

- simple CLI parsing from `ARGS`
- default parameter presets
- run metadata printing and output naming
- output README describing columns and assumptions

**Step 4: Run test to verify it passes**

Run: `julia --project=. test/runtests.jl`
Expected: PASS.

**Step 5: Commit**

This workspace is not a git repository. Skip commit.

### Task 5: Run end-to-end verification

**Files:**
- Verify only

**Step 1: Run unit tests**

Run: `julia --project=. test/runtests.jl`
Expected: PASS.

**Step 2: Run short Ising reproduction**

Run: `julia --project=. scripts/reproduce_dqpt_prl_2021.jl ising --steps 20 --dt 0.05`
Expected: output file created with nontrivial rate evolution.

**Step 3: Run short entanglement-focused reproduction**

Run: `julia --project=. scripts/reproduce_dqpt_prl_2021.jl xxz --steps 20 --dt 0.05`
Expected: output file created with finite entropy and rate columns.

**Step 4: Run a medium verification job**

Run: `julia --project=. scripts/reproduce_dqpt_prl_2021.jl all --steps 80 --dt 0.05`
Expected: both protocols finish and produce inspectable data.

**Step 5: Commit**

This workspace is not a git repository. Skip commit.
